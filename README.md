# 💊 Трекер лекарств

Веб-приложение для управления напоминаниями о приёме лекарств с автоматической отправкой уведомлений в WhatsApp.

---

## 📋 Содержание

- [Возможности](#-возможности)
- [Технологии](#-технологии)
- [Требования](#-требования)
- [Установка](#-установка)
- [Настройка](#-настройка)
- [Запуск](#-запуск)
- [Использование](#-использование)
- [API](#-api-endpoints)
- [Структура проекта](#-структура-проекта)
- [Green-API интеграция](#-green-api-интеграция)
- [Troubleshooting](#-troubleshooting)

---

## ✨ Возможности

### Управление пользователями
- ✅ Создание, редактирование, удаление пользователей
- ✅ WhatsApp номер в формате E.164
- ✅ Поддержка часовых поясов
- ✅ Выбор активного пользователя

### Управление лекарствами
- ✅ CRUD операции
- ✅ Обязательное фото лекарства
- ✅ Дозировка
- ✅ Активация/деактивация
- ✅ Загрузка изображений в Supabase Storage

### Расписание приёма
- ✅ Множество времён приёма для одного лекарства
- ✅ Формат 24-часа (HH:mm)
- ✅ Группировка по времени

### Циклы приёма
- ✅ Настройка периодов приёма/перерыва
- ✅ Дата начала цикла
- ✅ Автоматический расчёт текущей фазы
- ✅ Включение/отключение циклов
- ✅ Визуальные индикаторы статуса

### Инструкция по приёму
- ✅ Отдельная страница с полной инструкцией
- ✅ Все лекарства пользователя
- ✅ Расписание для каждого лекарства
- ✅ Статус циклов в реальном времени
- ✅ Возможность печати
- ✅ Ссылка для отправки пациенту

### Автоматические напоминания
- ✅ Cron scheduler (проверка каждую минуту)
- ✅ Отправка в WhatsApp через Green-API
- ✅ Учёт циклов (приём/перерыв)
- ✅ История уведомлений в БД
- ✅ Форматированные сообщения
- ✅ Обработка ошибок

### UI/UX
- ✅ Полностью на русском языке
- ✅ Адаптивный дизайн (Desktop, Tablet, Mobile)
- ✅ Оптимизация для сенсорных устройств
- ✅ Toast уведомления
- ✅ Современный чистый дизайн
- ✅ Нет авторизации (открытый интерфейс)

---

## 🛠 Технологии

### Frontend
- **Svelte 5.0** - реактивный фреймворк
- **Vite 5.0** - сборщик и dev сервер
- **Native CSS** - без фреймворков

### Backend
- **Fastify 5.0** - быстрый Node.js фреймворк
- **Node-cron** - планировщик задач
- **ES Modules** - современный синтаксис

### Database & Storage
- **Supabase** - PostgreSQL база данных
- **Supabase Storage** - хранение изображений

### Notifications
- **Green-API** - WhatsApp сообщения

---

## 📦 Требования

- **Node.js** 18+ 
- **npm** 9+
- **Supabase** аккаунт
- **Green-API** аккаунт (для WhatsApp)

---

## 🚀 Установка

### 1. Клонирование (если применимо)
```bash
git clone <repository-url>
cd medication-tracker
```

### 2. Установка зависимостей
```bash
npm install
```

---

## ⚙️ Настройка

### 1. Supabase Setup

#### Создать проект
1. Перейти на [supabase.com](https://supabase.com)
2. Создать новый проект
3. Скопировать Project URL и API keys

#### Запустить миграции
В **SQL Editor** выполнить по порядку:

```sql
-- 1. Создание таблиц
supabase/migrations/001_initial_schema.sql

-- 2. Таблица уведомлений
supabase/migrations/002_notifications_log.sql

-- 3. Настройка Storage
supabase/storage-setup.sql
```

### 2. Green-API Setup

#### Регистрация
1. Перейти на [green-api.com](https://green-api.com)
2. Зарегистрироваться
3. Создать инстанс

#### Подключение WhatsApp
1. Отсканировать QR-код в личном кабинете
2. Дождаться статуса "Авторизован"
3. Скопировать `idInstance` и `ApiTokenInstance`

### 3. Переменные окружения

Создать файл `.env` в корне проекта:

```bash
# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Backend
PORT=3000
HOST=0.0.0.0

# Frontend
VITE_API_URL=http://localhost:3000
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key_here

# Scheduler
ENABLE_SCHEDULER=true
CRON_TIMEZONE=Asia/Bishkek

# Green-API WhatsApp
GREEN_API_URL=https://api.green-api.com
GREEN_API_ID_INSTANCE=your_instance_id
GREEN_API_TOKEN_INSTANCE=your_token
```

---

## 🎯 Запуск

### Development (Frontend + Backend)
```bash
npm run dev
```

### Backend только
```bash
npm run dev:backend
```

### Frontend только
```bash
npm run dev:frontend
```

### Production Build
```bash
npm run build
```

### Доступ
- **Frontend:** http://localhost:5173
- **Backend API:** http://localhost:3000
- **Health Check:** http://localhost:3000/api/health
- **Инструкции:** http://localhost:5173/instructions.html?userId=UUID

---

## 📖 Использование

### Быстрый старт

#### 1. Создать пользователя
- Перейти на страницу "Пользователи"
- Нажать "+ Создать пользователя"
- Заполнить: Имя, WhatsApp (+996...), Часовой пояс
- Нажать "Создать"
- Нажать "Выбрать" для активации

#### 2. Добавить лекарства
- Перейти на страницу "Лекарства"
- Нажать "+ Добавить лекарство"
- Заполнить название и дозировку
- Загрузить фото (обязательно!)
- Нажать "Добавить"

#### 3. Создать расписание
- Перейти на страницу "Расписание"
- Нажать "+ Добавить расписание"
- Выбрать лекарство
- Выбрать время (например, 08:00)
- Нажать "Добавить"

#### 4. Настроить циклы (опционально)
- Перейти на страницу "Циклы"
- Нажать "+ Добавить цикл"
- Выбрать лекарство
- Указать дни приёма (например, 21)
- Указать дни перерыва (например, 7)
- Выбрать дату начала
- Нажать "Добавить"

#### 5. Посмотреть инструкцию
- Перейти на страницу "Превью"
- Нажать "📄 Открыть инструкцию"
- Откроется полная инструкция на отдельной странице
- Можно скопировать ссылку и отправить пациенту

#### 6. Получить напоминание
- Дождитесь времени приёма
- Автоматически придёт сообщение в WhatsApp
- Сообщение содержит: время, лекарства, дозы, статус циклов

---

## 🔌 API Endpoints

### Users
```
GET    /api/users              # Все пользователи
GET    /api/users/:id          # Один пользователь
POST   /api/users              # Создать
PUT    /api/users/:id          # Обновить
DELETE /api/users/:id          # Удалить
```

### Medications
```
GET    /api/medications/user/:userId     # Лекарства пользователя
GET    /api/medications/:id              # Одно лекарство
POST   /api/medications                  # Создать
PUT    /api/medications/:id              # Обновить
DELETE /api/medications/:id              # Удалить
POST   /api/medications/upload           # Загрузить фото
```

### Schedules
```
GET    /api/schedules/medication/:medicationId  # Расписание лекарства
GET    /api/schedules/user/:userId              # Расписание пользователя
GET    /api/schedules/:id                       # Одно расписание
POST   /api/schedules                           # Создать
PUT    /api/schedules/:id                       # Обновить
DELETE /api/schedules/:id                       # Удалить
```

### Cycles
```
GET    /api/cycles/medication/:medicationId  # Циклы лекарства
GET    /api/cycles/user/:userId              # Циклы пользователя
GET    /api/cycles/:id                       # Один цикл
POST   /api/cycles                           # Создать
PUT    /api/cycles/:id                       # Обновить
DELETE /api/cycles/:id                       # Удалить
```

### Instructions
```
GET    /api/instructions/:userId  # Полная инструкция пользователя
```

### Notifications
```
GET    /api/notifications/user/:userId    # История уведомлений
GET    /api/notifications/stats/:userId   # Статистика
GET    /api/notifications/all             # Все уведомления (админ)
```

### Preview (legacy)
```
GET    /api/preview/:userId?time=HH:mm    # Превью на время
```

---

## 📁 Структура проекта

```
medication-tracker/
├── app.js                      # Fastify сервер
├── package.json               
├── vite.config.js              # Vite конфигурация
├── svelte.config.js            # Svelte конфигурация
│
├── routes/                     # API routes
│   ├── users.js
│   ├── medications.js
│   ├── schedules.js
│   ├── cycles.js
│   ├── instructions.js
│   ├── notifications.js
│   └── preview.js
│
├── services/                   # Backend сервисы
│   ├── scheduler.js            # Cron scheduler
│   └── notifications.js        # WhatsApp отправка
│
├── utils/                      # Утилиты
│   └── cronJobs.js             # Cron конфигурация
│
├── supabase/                   # Database
│   ├── migrations/
│   │   ├── 001_initial_schema.sql
│   │   └── 002_notifications_log.sql
│   └── storage-setup.sql
│
└── frontend/                   # Svelte приложение
    ├── index.html              # Главное приложение
    ├── instructions.html       # Отдельная страница инструкций
    └── src/
        ├── main.js
        ├── instructions.js
        ├── app.css
        ├── instructions.css
        ├── App.svelte
        ├── InstructionsView.svelte
        ├── lib/
        │   ├── api.js          # API клиент
        │   ├── stores.js       # Svelte stores
        │   ├── Toast.svelte
        │   ├── ToastContainer.svelte
        │   └── toastStore.js
        └── pages/
            ├── UsersPage.svelte
            ├── MedicationsPage.svelte
            ├── SchedulesPage.svelte
            ├── CyclesPage.svelte
            └── PreviewPage.svelte
```

---

## 🔐 Green-API интеграция

### Что это?
Green-API - сервис для отправки WhatsApp сообщений через API.

### Быстрая настройка

#### 1. Регистрация
1. Перейти на https://green-api.com
2. Зарегистрироваться
3. Создать инстанс

#### 2. Подключение WhatsApp
1. В личном кабинете найти QR-код
2. WhatsApp на телефоне → Связанные устройства
3. Отсканировать QR-код
4. Дождаться статуса "Авторизован" (зелёный)

#### 3. Получить данные
В личном кабинете скопировать:
- `idInstance` (например: 7103123456)
- `ApiTokenInstance` (длинный токен)

#### 4. Добавить в .env
```bash
GREEN_API_URL=https://api.green-api.com
GREEN_API_ID_INSTANCE=7103123456
GREEN_API_TOKEN_INSTANCE=your_token_here
```

### Формат номера
- **В базе:** E.164 формат (`+996700112233`)
- **Для API:** chatId формат (`996700112233@c.us`)
- **Конвертация:** автоматическая

### Формат сообщения
```
💊 *Напоминание о приёме лекарств*

⏰ Время: *08:00*
👤 Пациент: Марат

📋 *Лекарства к приёму:*

1. *Такролимус*
   Доза: 1 таблетка (5 мг)
   🔄 День 5 из 21 (приём)

2. *Урсосан*
   Доза: 2 капсулы

_Не забудьте принять лекарства вовремя!_

🕐 Отправлено: 23.12.2024, 08:00:00
```

---

## 🗄️ База данных

### Таблицы

#### users
- `id` - UUID
- `name` - TEXT
- `whatsapp_number` - TEXT (E.164)
- `timezone` - TEXT
- `created_at`, `updated_at` - TIMESTAMPTZ

#### medications
- `id` - UUID
- `user_id` - UUID (FK)
- `name` - TEXT
- `dose` - TEXT
- `image_url` - TEXT
- `active` - BOOLEAN
- `created_at`, `updated_at` - TIMESTAMPTZ

#### schedules
- `id` - UUID
- `medication_id` - UUID (FK)
- `time` - TEXT (HH:mm)
- `created_at`, `updated_at` - TIMESTAMPTZ

#### cycles
- `id` - UUID
- `medication_id` - UUID (FK)
- `take_days` - INTEGER
- `rest_days` - INTEGER
- `cycle_start` - DATE
- `enabled` - BOOLEAN
- `created_at`, `updated_at` - TIMESTAMPTZ

#### notifications_log
- `id` - UUID
- `user_id` - UUID (FK)
- `channel` - TEXT (whatsapp, email, sms)
- `recipient` - TEXT
- `message` - TEXT
- `status` - TEXT (sent, pending, failed)
- `error` - TEXT
- `metadata` - JSONB
- `sent_at`, `created_at` - TIMESTAMPTZ

### Storage
- **Bucket:** `medication-images`
- **Access:** Public
- **Max size:** 5MB

---

## ⚡ Scheduler

### Как работает

#### Cron Job
- Запускается каждую минуту: `* * * * *`
- Проверяет текущее время (HH:mm)
- Находит все совпадающие расписания

#### Процесс
1. Получить все расписания на текущее время
2. Группировать по пользователям
3. Для каждого пользователя:
   - Загрузить циклы лекарств
   - Проверить фазу цикла (приём/перерыв)
   - Фильтровать: отправлять только в фазе "приём"
   - Сформировать сообщение
   - Отправить в WhatsApp через Green-API
   - Залогировать результат в БД

#### Логика циклов
```
Цикл: 21 день приём, 7 дней перерыв
День 1-21:   фаза "приём"   → ✅ отправить
День 22-28:  фаза "перерыв" → ❌ не отправлять
День 29:     новый цикл     → ✅ отправить
```

### Настройка

Включить/выключить:
```bash
ENABLE_SCHEDULER=true  # или false
```

Часовой пояс:
```bash
CRON_TIMEZONE=Asia/Bishkek
```

Доступные timezone:
- `Asia/Bishkek`
- `Asia/Almaty`
- `Europe/Moscow`
- `UTC`
- и другие (IANA timezone database)

---

## 🎨 Интерфейс

### Страницы

#### 1. Пользователи
- Список всех пользователей
- Создание, редактирование, удаление
- Выбор активного пользователя
- Индикатор активного пользователя

#### 2. Лекарства
- Grid карточек с фото
- Название, дозировка, фото
- Активация/деактивация
- Статус: Активно/Неактивно

#### 3. Расписание
- Группировка по времени
- Список лекарств для каждого времени
- Добавление множества времён
- Редактирование времени

#### 4. Циклы
- Список циклов с параметрами
- Текущий статус: "Приём - осталось X дн."
- Информация: дни приёма/перерыва, дата начала
- Включение/отключение

#### 5. Превью
- Кнопка "Открыть инструкцию"
- Кнопка "Скопировать ссылку"
- Описание возможностей
- Подсказки для пользователя

### Отдельная страница инструкций
- URL: `/instructions.html?userId=UUID`
- Информация о пациенте
- Все активные лекарства
- Расписание для каждого
- Статус циклов
- Кнопка печати (FAB)
- Полностью адаптивная

---

## 📱 Адаптивность

### Breakpoints
- **Desktop:** > 768px
- **Tablet:** 768px
- **Mobile:** < 480px

### Оптимизация
- ✅ Вертикальная компоновка на мобильных
- ✅ Полноширинные кнопки
- ✅ Адаптивные карточки (grid → 1 колонка)
- ✅ Минимальный touch-target: 44px
- ✅ Font-size 16px для inputs (предотвращает auto-zoom iOS)
- ✅ Запрет масштабирования (`user-scalable=no`)
- ✅ Отключение двойного клика для zoom
- ✅ Landscape режим
- ✅ Поддержка печати

---

## 🔧 Troubleshooting

### Backend не запускается

**Ошибка:** `supabaseUrl is required`
```bash
# Решение: проверьте .env файл
# Убедитесь что SUPABASE_URL заполнен
```

### Frontend не подключается к API

**Ошибка:** CORS или Network error
```bash
# Решение: проверьте что backend запущен
# Проверьте VITE_API_URL в .env
```

### Scheduler не отправляет сообщения

**Проверьте:**
1. `ENABLE_SCHEDULER=true` в .env
2. Green-API настроен правильно
3. Инстанс авторизован (зелёный статус)
4. Расписания созданы
5. Лекарства активны
6. Логи сервера

### WhatsApp не приходит

**Проверьте:**
1. Номер в формате E.164 (`+996...`)
2. Номер есть в WhatsApp
3. Green-API инстанс активен
4. Токены правильные в .env
5. Баланс Green-API положительный
6. Логи в БД `notifications_log`

### Изображения не загружаются

**Проверьте:**
1. Storage bucket создан
2. Bucket публичный
3. Policies настроены (storage-setup.sql)
4. Размер файла < 5MB

---

## 📊 Мониторинг

### Логи сервера
```bash
npm run dev:backend
# Смотрите консоль для логов scheduler
```

### История уведомлений
```bash
curl http://localhost:3000/api/notifications/user/USER_ID
```

### Статистика
```bash
curl http://localhost:3000/api/notifications/stats/USER_ID?days=7
```

### База данных
```sql
SELECT * FROM notifications_log 
ORDER BY sent_at DESC 
LIMIT 50;
```

---

## 🚀 Production Deployment

### Build
```bash
npm run build
```

Создаст `dist/` папку с:
- `index.html` - основное приложение
- `instructions.html` - страница инструкций
- `assets/` - JS, CSS, изображения

### Backend
```bash
node app.js
# или используйте PM2, Docker, etc.
```

### Environment Variables (Production)
```bash
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_key
PORT=3000
ENABLE_SCHEDULER=true
CRON_TIMEZONE=Asia/Bishkek
GREEN_API_URL=https://api.green-api.com
GREEN_API_ID_INSTANCE=your_id
GREEN_API_TOKEN_INSTANCE=your_token
```

### Frontend (Production)
Обновите `VITE_API_URL` перед build:
```bash
VITE_API_URL=https://your-api-domain.com
```

---

## 🔒 Безопасность

### Важно
- ❌ **Нет авторизации** (по требованиям)
- ✅ Открытый интерфейс
- ✅ UUID сложно угадать (128-бит)
- ✅ Токены в .env (не в коде)
- ✅ .gitignore для .env

### Рекомендации
- Используйте HTTPS в продакшне
- Не публикуйте ссылки инструкций открыто
- Мониторьте использование API
- Регулярно проверяйте логи
- Ротируйте токены Green-API

---

## 🧪 Тестирование

### Быстрый тест

1. **Создать тестового пользователя**
   - Имя: "Тест"
   - WhatsApp: ваш номер
   - Timezone: ваш часовой пояс

2. **Добавить тестовое лекарство**
   - Название: "Витамин C"
   - Доза: "1 таблетка"
   - Фото: любое изображение

3. **Создать расписание**
   - На текущее время + 2 минуты
   - Например, если сейчас 14:30, создайте на 14:32

4. **Дождаться уведомления**
   - Через 2 минуты должно прийти сообщение в WhatsApp

5. **Проверить логи**
   ```bash
   # В консоли должно быть:
   🔔 Checking reminders for 14:32...
   ✓ Message sent successfully!
   ```

---

## 📞 Поддержка

### Проблемы с Green-API
- Документация: https://green-api.com/docs/
- Поддержка: support@green-api.com

### Проблемы с Supabase
- Документация: https://supabase.com/docs
- Discord: https://discord.supabase.com

---

## 📝 Лицензия

ISC

---

## 🎯 Roadmap

### В разработке
- [ ] Push-уведомления (PWA)
- [ ] Offline режим
- [ ] История приёма лекарств
- [ ] Подтверждение приёма
- [ ] Email уведомления (fallback)
- [ ] Экспорт в PDF
- [ ] QR-коды для быстрого доступа
- [ ] Статистика приёма
- [ ] Отчёты для врачей

---

## 👥 Использование

Это приложение создано для:
- **Пациентов** - автоматические напоминания о приёме лекарств
- **Врачей** - управление расписанием пациентов
- **Родственников** - контроль приёма лекарств близких
- **Клиник** - массовое управление пациентами

---

## 🙏 Благодарности

- **Svelte** - за отличный фреймворк
- **Fastify** - за быстрый backend
- **Supabase** - за простую БД
- **Green-API** - за WhatsApp интеграцию

---

**Создано с ❤️ для здоровья людей**
